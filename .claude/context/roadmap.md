---
type: roadmap
---

# Roadmap

In-flight + planned work, grouped **by concept** (one `##` per initiative / area / standalone item). Read first for parallel-session sync.

**Legend:** `- [x]` done (+ `— YYYY-MM-DD`) · `- [~]` in progress (+ `branch` / `worktree`) · `- [ ]` queued · `- [!]` blocked (+ reason). One status per line — never in prose, never mixed in one bullet. **Concepts with a `[~]` leaf sort to the top** — that's where a parallel session looks for what's hot, and the `branch` / `worktree` on each `[~]` leaf is how it avoids collisions.

## Demo site — laravel-vue-demo

- [x] **Demo repo built and pushed** — `dev-applied/laravel-vue-demo`, the template with all 28 modules installed, plus the two files that make it a demo rather than a copy. `DemoGatesServiceProvider` grants every module ability (the modules gate themselves fail-closed on purpose, so a bare install answers "This action is unauthorized" on most screens — correct, and a useless demo); it boots only in local/demo/staging, lists abilities explicitly so a new module's missing ability shows as a 403 rather than being silently granted, and excludes impersonation deliberately. `ShowcaseSeeder` fills the registry-driven surfaces. Both verified against a running stack before extraction: `/announcements` went from 403 to showing seeded content — 2026-08-25
- [x] **The 8 workflow files would not register — they were in the repo's FIRST commit.** `actions/workflows` returned `total_count: 0` while the files were correct on the default branch and repo-level Actions reported `enabled: true`. Not private visibility (`laravel-vue-modules` is private and registers), not CRLF, not the reusable-workflow reference (plain `ci.yml` failed to register too). Isolated by pushing a hello-world workflow in a LATER commit: it registered immediately, 1 of 9. Touching the other eight in a follow-up commit registered all of them. Worth knowing for every repo created this way — a `gh repo create` + single initial push leaves its workflows invisible — 2026-08-25
- [x] **Three deploy blockers found and fixed by running it, none of which named itself.** (1) **Workflows in a repository's FIRST commit never register** — `gh workflow list` empty while Actions reported `enabled: true` and the files were correct on the default branch; isolated by pushing a hello-world workflow in a later commit, which registered instantly. Touching the other eight registered them. (2) **`PHP_VERSION` is a repository VARIABLE**, not a workflow input (→ `FPM_VERSION` → `'8.1'`); the template needs `php ^8.4` and composer blamed `brick/math` and `laravel/framework` instead. (3) **`NODE_VERSION` defaults to `16.19.0`**, which cannot build Vite 7 — a bare "Compile CSS and Javascript" failure naming nothing. All three now pinned or documented in the template's own deploy workflows and README, so no bootstrapped project rediscovers them. The build job now passes end to end: artifact created and stored — 2026-08-25
- [!] **Demo deploy stops at SSH: `error: missing server host`.** Everything up to the server is green. The `DEV` environment has no `HOST` (and almost certainly no `USERNAME` / `SSHKEY`); `secrets: inherit` is resolving something at org level, since the Slack notify step succeeded, but not the host. These are per-project server credentials and are not mine to create or ask for. blocked-on: Devin — copy the `DEV` environment secrets from an existing deployed project onto `dev-applied/laravel-vue-demo`, then re-run Deploy Dev. Nothing else is in the way

## SmsMessaging — module 26


- [x] **SMS as a channel, with the opt-out handling that is a legal duty.** 10 distinct projects carry a hand-rolled Twilio integration. Installs with no vendor account — the default driver logs, so opt-out handling and a delivery log work before anyone signs up to anything. Nothing calls a driver directly: normalise to E.164, check the opt-out list, log the attempt, all in `SmsManager`, because a rule enforced per-driver is missing the day somebody adds the second driver. Honouring STOP is a TCPA/CTIA obligation, and numbers are normalised before anything compares them — the failure otherwise is a message to somebody who texted STOP, because a form captured "(555) 123-4567" and a string comparison says that differs from +15551234567. 35 tests on the default variant, 39 on `driver=twilio`, 11 mutations all caught — 2026-08-25
- [x] **The Otp bridge, and the bug only the real path found.** Otp declares `otp.channel.sms` and knows nothing about a vendor; this binds it. The first version deliberately did NOT implement Otp's `OtpChannel` interface, reasoning the class must load whether or not Otp is installed — and every SMS code request 500'd, because `ChannelRegistry::get()` has that return type and a wrong one is a TypeError. Its own test missed it by calling the class directly instead of through the registry the application uses. Now implements the interface, resolves through the registry, and posts to `/otp/request` end to end — 2026-08-25
- [x] **Codes are redacted in the delivery log.** The handset gets the digits, the log gets `Your verification code is ••••••`. Support reads that table while a code is still live, and a code is a credential — 2026-08-25
- [x] **A 403 with no message left the page lying.** `view-sms-log` falls closed, but the empty refusal let the page render its "Nothing sent yet" empty state — telling the user there are no messages when the truth was that they may not see them. Found by loading it — 2026-08-25
- [x] **`module:configure` left the previous variant's env keys behind.** Switching SmsMessaging from `driver=twilio` back to the default left `TWILIO_*` in `.env` — harmless for a log driver, not harmless for an auth or billing variant where a stale key still RESOLVES and something still reads it. `apply()` now takes the outgoing selection (the command already had it) and retires the keys only that choice declared. Commented rather than deleted, deliberately: the value may be a credential the developer pasted in, and a tool that silently destroys one is worse than the stale key it is solving — commenting stops it resolving, which is the actual hazard, and leaves it recoverable. A key the incoming choice also declares is left alone, so the outcome does not depend on write ordering; a fresh install (`module:add` passes no previous) retires nothing. 4 tests, 4 mutations caught — 2026-08-25
- [x] **`module:check` now catches rsync-over-an-installed-module.** It bit three times in one night: syncing a module's full source over an installed copy silently reintroduces the files its option variant dropped, and the failure surfaces as unrelated tests going red — a reinstated `StepUpTest.php` produced seven 405s that read as a broken route. Nothing else could notice, because every other check reads the manifest and the manifest still says `login`. The new check compares each installed variant's drop list against what is on disk. NOT behind `--defaults`: a project picks its variants on purpose so option drift there is normal, but a stray file is wrong in a project for exactly the same reason. 3 tests, 3 mutations caught, and verified against the actual accident — 2026-08-25

## Sweep 2026-08-25 — authorization, deferrals, tenancy


Two parallel read-only sweeps over all 22 modules (deferrals/TODOs; per-user data
scoping). Every claim verified by hand before acting — one was wrong (Invitations'
"membership oracle" is already deliberately no-opped, with a comment saying so).

- [x] **Tasks shipped with no authorization at all** — every route bare `auth:sanctum` over a table with no owner column. Proven by driving it: a signed-in stranger listed, read, retitled another user's task to "Hijacked" and DELETED it, all 200. Tasks was the only module of 22 without a gate, and nothing in its README claimed that was deliberate. Now: reads governed by a `TaskScope` seam (permissive default — a shared board is the common shape, and it is only safe as a default because the destructive paths do not depend on it); move stays open to anyone who can see the task, because dragging someone else's card is the point of a board; edit needs creator/assignee/`manage-tasks`; delete needs creator/`manage-tasks`, since being given a job is not permission to destroy the record of it. `manage-tasks` falls CLOSED when undefined — flipping that one `return false` reddens four tests. Re-verified after: 403/403, title and row intact. Why the suite never noticed is the more useful half: tests built tasks with a factory, a factory runs outside a request, so WhoDidIt recorded no creator and there was nothing for a guard to check. 6 tests, mutation-checked four ways — 2026-08-25
- [x] **Files silently discarded `folder_id` on every upload** — both paths sent it, the server had no column, no fillable entry and no rule. The README claimed it was "threaded through the upload path" while the column did not exist. Now recorded on both paths, exposed, indexed; deliberately not a FK and no folders table, because the module does not own that concept — 2026-08-25
- [x] **The DataImport disk-failure regression guard was committed skipped** — the fix shipped, the thing stopping it recurring did not. `UploadedFile::store()` delegates to `putFileAs()`, so that is the one call the double must fail. Un-skipped, mutation-checked — 2026-08-25
- [x] **Notifications was the only shipped module with no README** — 21 of 22 had one; the bell's two-component split, the 60s poll default and the drop-in instruction lived only in a docblock — 2026-08-25
- [x] **Files has no virus-scanning seam and no storage quota** — both now have one place to live, without the module shipping an engine or a cap. `FileScanner::refuse()` returns null-or-reason, `NullFileScanner` refuses nothing (pinned). The two storage options refuse at genuinely different moments and the README says so: `local` before any bytes are written, `s3-presigned` in `process()` — the browser PUT straight to the bucket, so refusing there means DELETING what landed rather than preventing it, and a project needing the bytes never to land should scan at the bucket instead. `config('files.quota_mb')` caps total storage per uploader (413), null by default. 6 tests, mutation-checked three ways — 2026-08-25
- [x] **CI went red for three commits and the reported cause was in the wrong module.** The template bundles all 22 modules, so every CI leg runs every bundled module's tests. `modules/Files` was committed while `imagine/imagine` was not — `module:add` had installed it into my working copy and composer.json was never committed — so legs installing Auth and Announcements failed with `Class "Imagine\Imagick\Imagine" not found`. Two fixes, both structural: the deps are committed (`imagine/imagine`, `laravel/mcp` — the template bundles `modules/Auth/Mcp/AppServer.php` which references it), and a new `module:check` command compares every installed module's declared composer/npm requires against composer.json and package.json, reading `installed_options` so a module offering three backends is not asked to have all three. Wired into BOTH CI workflows before the suite so the cause names itself. Also fixed the Files imagick skip guard, which checked `extension_loaded('imagick')` while the code needs the `imagine/imagine` COMPOSER package — CI installs the extension, so the guard was satisfied while the library was absent and the test fataled instead of skipping. 4 tests — 2026-08-25
- [x] **Template CI never ran on tonight's commits** — `ci.yml` has `push: branches-ignore: [main, master, ...]` by design, so direct-to-master pushes trigger nothing. Dispatched it manually on master: success, so eslint/vue-tsc/vitest/Capacitor-smoke all pass on the run's work. Filed in known-issues.md with the workaround, since the branches-ignore is correct for the normal PR workflow and only bites this run's authority level — 2026-08-25
- [!] **Tenancy: 8 modules have no scoping seam and rows with no owner column.** Ranked by where cross-tenant leakage would actually occur: Tasks (now has one), Support (tickets carry reporter name/email/body plus internal staff replies; `can:manage-support` is a role, not a boundary), FormBuilder (`form_submissions.answers` is arbitrary PII by definition, and `forms.slug` is globally unique so tenants collide and the PUBLIC render/submit endpoints are tenant-blind), Booking (same globally-unique slug problem, plus capacity is computed across the whole table so one tenant's bookings consume another's slots), AuditLog (`old_values`/`new_values` are literal row snapshots, so one `viewAuditLog` grant reconstructs other tenants' data for tables this module never touches), Invitations, Announcements' admin CRUD (the READ path is genuinely covered by AudienceResolver), Users, and Tags' merge (which rewrites `taggables` rows belonging to every tenant — destructive rather than merely leaky). Genuinely isolated and needing nothing: Favorites, Exports, DataImport, Notifications, Billing, Auth — every query filters `user_id` from `$request->user()` rather than the wire, and Exports/DataImport hand the query closure itself to the project. blocked-on: this is the same call the "Tenancy sequencing" leaf below records as Devin's. Not a bug list — every one of these is behind a project-defined gate today. It is a list of what a multi-tenant project would have to retrofit, and it exists so the decision is cheap rather than free to ignore.

## Deploy credentials — secrets over variables


GitHub **variables** are readable by every repo collaborator and are not masked in logs, so a project on variables exposes its deploy key and full production `.env`. `dev-applied/deploy-action` reads `secrets.X || vars.X` for every sensitive value (its PR #7, 2026-08-24) — but that path is **dead unless the caller workflow passes `secrets: inherit`**. Detail + migration procedure: the `client-deploy` runbook, credentials page.

- [~] `secrets: inherit` on the four deploy callers, + secrets-fallback and log-safe env handling in `deploy-lambda.yml` — feature/deploy-secrets-inherit
- [ ] Per-repo migration: create environment secrets → confirm one deploy → delete the variables → **rotate** (values that lived in variables are presumed disclosed) — depends-on: visilaunch/Vaultwarden GitHub-secrets sync
- [x] Rotation tiering rule + migration playbook in the `client-deploy` runbook — 2026-08-24
- [x] Truth-up: runbook named the decommissioned Infisical as canonical store; corrected to visilaunch/Vaultwarden — 2026-08-24

## Modules — CI + distribution harness


The `dev-applied/laravel-vue-modules` repo and the `module:add` / `module:configure` / `module:outdated` flow. Contract: `docs/modules.md`. Green CI = safe to `module:add`.

- [x] Drop `TEMPLATE_REPO_TOKEN` — template repo is public; empty token failed checkout — 2026-08-24
- [x] Drop `COMPOSER_AUTH` — nothing in composer.lock resolves from the private registry — 2026-08-24
- [x] Build the frontend in CI so module Vue halves are actually compiled — 2026-08-24
- [x] Clear template-bundled module copies before `module:add` (Example collision) — 2026-08-24
- [x] Module plugin glob — modules can now ship `resources/ts/plugin.ts` to register globals / Vue plugins — 2026-08-24
- [x] `npm_requires` — modules can declare npm deps; recorded in package.json when node is off PATH — 2026-08-24
- [x] `module:add` prunes directories an option drop leaves empty — an empty `Mail/` reads as a broken install — 2026-08-24
- [x] `module:update` — the three-way merge from docs/modules.md, automated. `git merge-file` per file; `installed_options` replayed against both upstream trees so a pruned variant stays pruned; upstream deletions reported, never applied; conflicts exit non-zero. 10 tests against a real git repo, plus an end-to-end run with a genuine overlapping edit — 2026-08-24
- [x] `bin/lint` in the modules repo — pint AND eslint the whole repo from a template checkout in ONE direction, so a later rsync cannot silently revert the fixes. The round trip ate pint fixes once and eslint fixes once; covering only pint just moved which CI leg went red — 2026-08-24
- [x] `module:make` — scaffolds the full module anatomy from stubs that encode 21 modules' worth of review (newFactory, grouped search, through(), sometimes-on-update, ungated Vue route, flex-nowrap actions). Proven by generating a module and running it through the real pipeline: installs, 8 tests pass first try, pint/eslint clean, builds, vue-tsc 0, page loads in a browser — 2026-08-24
- [!] **Resource key casing is split-brain** — kernel snake_case (`due_date`, `owner_id`, `full_name`) vs camelCase in all 13 modules (`dueAt`, `isSelf`). A project installing modules gets a mixed API. blocked-on: **Devin's call, not sweepable unilaterally.** Both directions cost real work and neither is obviously right: making the kernel camelCase breaks its alignment with the Wayfinder/`models.d.ts` types, which are generated from the PHP models and are therefore snake_case, and forces AuthUserResource to hand-list fields rather than pass the model; making the 13 modules snake_case touches ~340 tests and every module frontend. Note either way that REQUEST payloads must stay snake_case regardless — form field names have to match the FormRequest rule keys that `getErrors('due_date')` looks up — so the outcome is an asymmetry (snake in, camel out) or a full snake_case sweep.
- [x] `module:add` now clears `node_modules/.vite`. A plain dev-server restart keeps the Vuetify plugin's cached virtual modules, so after a module changed the globs every component 404'd on its `.sass` and the app rendered blank with nothing naming the cause — three debugging cycles before the pattern showed — 2026-08-24
- [x] `@vue/test-utils` added and the frontend testing conventions written into `resources/ts/CLAUDE.md` — jsdom is not the default environment, mount() not createApp, Vuetify components need the plugin in global.plugins so prefer a stub — 2026-08-24

## Modules — QA sweep findings (three parallel audits, 2026-08-24)


Three read-only sweeps over all 22 modules — authorization, frontend correctness, data integrity.
Every CERTAIN claim is verified by hand before it is worked; the impersonation one was, and was real.

**Root cause behind several of these:** the suite runs on SQLite `:memory:` (`phpunit.xml:29-30`, and
`module-ci.yml` installs only `pdo_sqlite`), while the project's own CLAUDE.md says Feature tests use
the project's MySQL. So nothing MySQL-specific and nothing concurrent is testable, which is exactly
how tonight's `timestamp` bug reached main — the Booking migration that outright FAILS on MariaDB
passes CI green.

- [x] **Auth — impersonation was ungated.** `auth:sanctum` and nothing else on an endpoint that mints a bearer token for any user id: one request, any account, including admins. Zero tests existed. Now fail-closed on `impersonate-users`, token bounded by TTL, every use logged — 2026-08-24
- [x] **CI + phpunit run SQLite, not MySQL** — flipped both to MySQL; the template's CI had been standing up a MySQL service all along that phpunit's env block silently overrode. 520 tests pass on the real engine with all 22 modules installed, and the two schema-assertion regressions that had been SKIPPING now run. Pinned to a SEPARATE `laravel_template_testing` database, since RefreshDatabase drops every table it finds — 2026-08-24
- [x] **Files — no authorization at all.** Fixed behind a `FileAccess` seam defaulting to owner-only; refusals are 404 so sequential ids stay unenumerable; `process` gated too; module README written (Files was the only module without one). 7 tests, mutation-checked, both storage variants green — 2026-08-24
- [x] **Invitations — self-invite privilege escalation.** Gated fail-closed on `manage-invitations`; `role` must name an existing role AND be one the inviter holds unless they pass `assign-any-role` — otherwise "may invite" quietly means "may create a super-admin". 6 tests, mutation-checked — 2026-08-24. ORIGINAL: Route is `auth:sanctum` only, and `role` validates as a free string never checked against the roles table. Any user invites themselves as `super-admin` and accepts it. The controller docblock claims an `invitations.manage` gate that does not exist anywhere.
- [x] **Support — open ticket queue, and the reply endpoint is a mail relay.** Both behind `manage-support`, fail-closed; the public submission endpoint deliberately stays open and a test pins that. 5 tests, mutation-checked with `Mail::assertNothingSent()` so the relay is proven closed rather than merely 403'd — 2026-08-24. ORIGINAL: No gate on index/show/update/destroy, so any user reads every ticket. Worse: `TicketReplyController::store` has no gate and mails an arbitrary body to the ticket's email, from our domain, into a thread the customer already trusts.
- [x] **Booking — anyone can cancel any booking, and the public lookup leaks PII.** Cancel needs the booking email as a second factor (wrong email and unknown reference are indistinguishable); a PublicBookingResource drops name/email/notes; reference regenerated uniformly instead of folding 62 symbols onto 36; and approve() got the transaction+lock book() already had. 6 tests — 2026-08-24. ORIGINAL: The reference is the sole credential for a destructive change, and the public endpoint returns the ADMIN resource (name, email, 2000-char notes).
- [x] **Announcements — forged acknowledgement.** `dismiss()` now applies the same `live()` + audience filters `index()` does, refusing with 404; and the acknowledgement timestamp is stamped once instead of being rewritten by every later call. 4 tests, mutation-checked — 2026-08-25. ORIGINAL: `dismiss()` applies neither the `live()` scope nor the audience resolver that `index()` does, so any user can acknowledge a draft or an announcement targeted at someone else. `requires_acknowledgement` exists precisely to be a defensible record.
- [x] **SavedViews — screen-key enumeration.** `SavedViewScope::allows($key, $user)` checked on both index and store, refusing with 404. The shipped NullScope stays permissive by design — the module cannot know which screens are privileged, and a default that refused unknown keys would make saved views vanish on install untraceably; the README says so and shows the implementation. 3 tests — 2026-08-25. ORIGINAL: No allow-list of keys and no ability check, so guessing `admin.users.index` returns every shared view's payload and owner name.
- [x] **Files — `time()` filenames collide and silently overwrite.** `Str::random(8)` on both call sites; test reads paths from the DB because FileResource deliberately hides them — 2026-08-25. ORIGINAL: Same-named files uploaded in the same second share a path; N rows, one object. Deleting any one unlinks the bytes for all of them. A phone posting four `image.jpg` hits this on the first try.
- [x] **Support — reference space is 36^6 with no collision retry.** 8 chars from a 32-symbol look-alike-free alphabet (1.1e12), plus `createWithReference()` retrying on the index. The alphabet now does what the original comment promised — 2026-08-25. ORIGINAL: ~1% collision by 6,600 tickets, and a collision 500s the PUBLIC contact form with the message lost.
- [x] **Billing — no event ordering guard.** `user_entitlements` now carries a millisecond watermark of the event its access was resolved from, and `EntitlementWriter` — the only thing that writes access — refuses a strictly-older write. Two judgement calls, both tested: equal timestamps both apply (a transfer emits a same-millisecond pair, and ordering cannot separate those anyway), and a patch read LIVE from the RevenueCat API is stamped with the READ time, not the event's — `TransferResolver::receive()` never reads the payload, so ordering it behind the event would make a delayed TRANSFER silently stop working. An event with no usable timestamp applies but does not advance the watermark, because treating unplaceable as `now` discards everything still in flight behind it. 6 tests, mutation-checked four ways — which also caught a flaky test of my own that read the clock twice — 2026-08-25. ORIGINAL: Nothing reads `event_timestamp_ms`; last-received wins. A reordered EXPIRATION after an INITIAL_PURCHASE leaves a paying customer at `tier=free`.
- [x] **Realtime's broadcast-auth guard never reached the template either.** Found by sweeping all 28 modules for template/modules-repo divergence after the typing drift. `module.json` differs everywhere by design (module:add records the installed variant); Realtime was the only module with real content drift, in `Routes/api.php` and its test. The template still had the rubber-stamp `broadcasting/auth`: Laravel's `null` broadcaster is the default in a fresh install AND in the test environment and authorises every channel, so every project bootstrapped from the template inherited an open endpoint — measured in the running app, a signed-in user POSTing an undeclared private channel got 200. Synced, 747 passing — 2026-08-25
- [x] **`.modules-src` had gone stale on three files.** It is what `module:add --from=.modules-src` installs from, so a freshly bootstrapped project was getting old versions while both the template's installed copy and the modules repo had moved on: `Checklists/AppChecklistPanel.vue`, `Realtime/RealtimeConfigController.php`, `Realtime/config/realtime.php`. Whole tree now at zero non-`module.json` drift — 2026-08-25
- [x] **Render-verified AdminLayout — it had never been displayed.** No route uses it (`layouts/index.ts` auto-registers every `.vue` in the folder, so it was registered and compiled but never mounted), which is how one of the ten kit components shipped broken. Mounted it behind a throwaway route: app bar, nav drawer, collapse control and page content all render, no boundary trip, no horizontal scroll. Its AppErrorBoundary wiring is now driven rather than merely compiled — 2026-08-25
- [x] **AdminLayout's nav drawer rendered empty with no explanation.** `navItems` defaults to `[]`, so every project on day one got a blank column and no clue a prop was missing — the same shape as the DataImport wizard empty state fixed earlier tonight, and the Dashboard on the same screen already explains its own. Now says "No navigation items. Pass a nav-items prop, or override the #nav slot.", hidden in rail mode. Verified by mounting the layout behind a throwaway route and reading it on screen — 2026-08-25
- [x] **"Empty with no explanation" — four of five instances now fixed.** DataImport's wizard, AdminLayout's drawer and Settings all shipped a surface that is legitimately empty until a project registers something, saying nothing about it. Settings was the worst: `/settings` rendered 61 characters of text in a working app — a card with an empty tab strip and no clue that registering was the missing step. Now carries an AppEmptyState naming the real call. Writing that message caught a second bug in itself: my first draft said `SettingsRegistry::group()`, and the actual API is `SettingRegistry::add()` — singular, different method — so the hint would have sent developers to something that does not exist. Verified on screen; synced to modules repo and .modules-src. Remaining: `Onboarding/OnboardingPage.vue` — 2026-08-25
- [x] **Onboarding page had no empty state — and contradicted itself when empty.** Last of the five. `v-for="step in state.steps"` with no `v-else`, so an install with nothing registered got a progress bar reading "0 of 0 done" over an empty list. Fixing it surfaced a second bug: `state.complete` is true when nothing required is outstanding, which is ALSO true when nothing is registered, so the page showed "No onboarding steps registered yet" and "You are all set — everything required is done." one above the other. Both paths driven in the browser — steps unregistered to see the empty state, then restored to confirm the populated list still renders — 2026-08-25
- [x] **`/settings` rendered "No settings registered yet" over a 403.** Noticed while verifying the empty state added an hour earlier — which made it worse, because the generic error path leaves `groups` empty and an empty `groups` renders that message. So the page told the user nothing was configured when the truth was they may not see it. Exactly the bug the SMS log page had, fixed the same way: the composable tracks `forbidden` and handles 403 before the generic path, and the page shows "You cannot read the settings" instead. The gate question — who should hold the ability — is still Devin's; this is the page no longer lying about it either way. Verified on screen — 2026-08-25
- [x] **`module:check` now catches `.modules-src` ↔ `modules/` content drift.** It bit three times in one night, each the same shape: a fix lands in one copy and the other silently keeps the old file. Nothing fails while they disagree, because the installed copy is the one that runs — the cost is paid by the NEXT project bootstrapped from the template, which `module:add --from=.modules-src` hands the stale version. Realtime's broadcast-auth guard sat that way for a night: corrected in `modules/`, still rubber-stamping every channel in the source a new project would receive. Compares only files present in both trees (absent on one side is the drop list working, which `strays()` already covers) and excludes `module.json`, which `module:add` writes `installed_options` into by design. Silent when `.modules-src/` is absent, so a bootstrapped project is unaffected. 3 tests, 3 mutations caught — two of them redden 11 tests each, which is the false-positive protection proving itself — 2026-08-25
- [x] **Realtime's pusher typing fix never reached the template, reddening every module's CI.** The correction landed in the modules repo at 224e49f and the template's installed copy stayed on the deprecated boolean signature. Module CI checks out template `master` and type-checks the assembled app, so EVERY module's job tripped over the stale file — the red run on 6b8325e was this, not the impersonation change in it. Synced into both `modules/` and `.modules-src/`; full unfiltered vue-tsc on the template is now 0 errors. Found only because module CI runs on push and the template's does not — 2026-08-25
- [!] **The template gets no automatic CI on direct-to-master commits.** `.github/workflows/ci.yml` has `push: branches-ignore: [main, master, staging, dev]`, and its comment gives the reason: "Pushes to the long-lived branches are already covered by the PR that merged into them." That premise is true under a PR workflow and false under the direct-to-master one agreed for this run — so nothing pushed tonight was checked until dispatched by hand with `gh workflow run CI --ref master`, and a real type error sat on master for hours as a result. The modules repo does trigger on push to main, which is the only reason it was caught. blocked-on: Devin — this is a template-wide policy call, not a bug to fix unilaterally. Either drop `master` from branches-ignore, or keep the PR workflow and treat direct-to-master as the exception it was. Note the `pull_request` half of that block is unrelated and should stay as-is: it deliberately has no branch filter so bootstrapped projects on main/dev/staging still get PR checks
- [x] **AppErrorBoundary was mounted nowhere.** A working error boundary no layout used, so an uncaught render error in any page took down the whole Vue tree and left a blank white app. Wrapped the routed slot in Default, Empty and Admin, each tagged with its layout name so Sentry groups by boundary. Verified with a page that throws during render: fallback shown in place of the page, normal pages unaffected. Does NOT catch a failed dynamic import of the route component — that happens during navigation, before the boundary exists — 2026-08-25
- [x] **AppDateRangePicker never imported its own date inputs.** It rendered the preset chips (Today, Last 7 days, YTD…) and then nothing where From/To belong. `<script setup>` resolves a child only if the file imports it, and `AppDateInput` never was; Vue reports that as a WARNING, so the build, type-check and tests were all green while the component was half-missing on screen. Found by mounting all ten never-rendered kit components on one throwaway page and looking — nine were fine. Wrapping each in its own boundary isolated the failure in a single load — 2026-08-25
- [x] **Guard: components used but never imported.** Seventh check in `bin/lint-kernel`, mirrored into the modules repo, born from the AppDateRangePicker bug above. Two false positives fixed before wiring it in, both the same shape as the `theme--light` one earlier in this run — matching a substring in prose: the block regex matched the words "<script setup>" inside a COMMENT, and `import Foo, { FooProps } from` was not read as importing Foo. Verified both directions: removing the real import reproduces the finding, restoring it goes clean — 2026-08-25
- [x] **Nested impersonation destroyed the real admin's token.** Self-review of the above. Impersonating while already impersonating parked the impersonation token over the real person's, so their own token was gone for good and Stop returned them to the previous impersonated user rather than to themselves. Narrow — it needs a gate permissive enough that the impersonated user also passes it — but it silently destroys the one credential the park/restore mechanism exists to protect. The stash now refuses to overwrite. Driven both ways in the browser: after a nested impersonation the stash still held the original admin's token and Stop returned to that admin — 2026-08-25
- [x] **Auth — the impersonation banner was mounted nowhere, and "stop impersonating" logged you out.** Three things had to be true and none were. GET /auth never reported the session as an impersonation, so no page could show the shipped `AppImpersonationBanner` — now read from the token's abilities with `in_array`, not Sanctum's `can()`, which answers true for any ability on the `*` wildcard every ordinary login token holds. The store waited for an `access_token` the stop endpoint never returns (it only deletes the impersonation token), so the browser kept presenting a destroyed token and 401'd the admin out of the app entirely; the caller's own token is now parked in a second storage slot and restored on stop, persisted so a reload doesn't strand it, read-and-cleared, and dropped on logout so signing out mid-impersonation doesn't leave an admin bearer token behind. And the banner rendered 270px tall — Vuetify 4's base `.v-banner` carries `flex: 1 1`, which grows TALL as a direct child of the column-flex `v-app`; fixed once in `resources/scss/VBanner.scss` because `AppNetworkBanner` is the same shape and carries the same latent bug, invisible only because it shows when you're offline. Banner also converted to Options API and made responsive — CORRECTION to that commit message: it claimed this codebase uses Options API everywhere, which is false (17 of 25 kernel components are `<script setup>`, and the convention is Options for pages/layouts, setup for leaf components). I read the stale CLAUDE.md copy in session context instead of the current file. The component works either way and is verified; the claim was wrong, not the code — at 390px `lines="one"` clamped to "Impersonating as…", cutting the one fact the banner exists to show. Browser-verified end to end: impersonated, banner correct, clicked Stop, came back as the admin on the same page with the parked token restored — 2026-08-25
- [x] **DataImport — double dispatch and replay-on-retry.** The controller now claims the import with a compare-and-swap (only `uploaded`/`failed` may start), backed by `WithoutOverlapping` for anything that enqueues directly; `processed_rows` records the last line finished with, so a retry resumes and seeds its counters from the previous attempt rather than restarting at line 1 and reporting only the tail. Checkpoint every 100 rows, so the residual replay is bounded rather than eliminated — documented, and `handle()` has always received the line number so a target can upsert. Marking the import failed moved out of the catch (which ran on every attempt, flipping the UI to failed while retries were still queued) into `failed()`. Re-mapping an import with committed rows is refused. Also fixed the wizard's missing empty state — the registry is an allow-list, so a fresh install has no targets and the selector rendered empty with no explanation. 9 tests, mutation-checked five ways. Browser-verified: upload→map→preview→import writes 3/3, a re-run answers 409, and four SIMULTANEOUS starts return one 202 + three 409s with no duplicate rows — 2026-08-25. ORIGINAL: No in-flight guard, and a throw re-enters the CSV at row 1, so a worker killed at row 18k re-imports 18k rows.
- [x] **Booking — `approve()` has neither transaction nor lock** (shipped with the above) —, while `book()` twelve lines up has both. Two approvals on a capacity-1 slot both pass the check and both confirm.
- [x] **Users — the last-active-admin guard is a check-then-act.** `protecting()` wraps the check and write in one transaction with `lockForUpdate`; token revocation moved inside too — 2026-08-25. ORIGINAL: Two admins deactivating each other concurrently both see "1 remaining" and both write. Zero active users, no in-app recovery.
- [x] **Announcements — publish double-dispatch, and the email job re-sends from recipient 1 on retry.** Both routes to mailing the whole audience twice now stop at a unique index rather than in application logic: publish is a compare-and-swap on the UPDATE, and `announcement_deliveries` holds one row per address, claimed via `insertOrIgnore` BEFORE the send. Claimed before rather than after on purpose — a crash in that gap costs one person one email and they still see it in-app, while a crash the other way round mails everyone again; at-most-once is the right guarantee for a broadcast, which is also why a throwing send does not release its claim. Consequence stated in the README: unpublish-then-republish no longer re-mails, because that flow corrects a typo rather than requesting a re-send. The `in-app` option drops the new migration along with the job and mailable. 5 tests, mutation-checked three ways including the inverse (a claim that always says "already sent" must redden the tests proving people DO receive mail). Both variants pass — 2026-08-25. ORIGINAL: The job's own comment states the bug.
- [x] **Tasks — status transitions validated then written unconditionally.** Compare-and-swap on both `update` and `move`; zero rows is a 409 — 2026-08-25. ORIGINAL: Two drags on the same card mean the transition that actually happened was never validated.
- [x] **Otp — the attempt cap is bypassable by concurrency.** Both the cap and the consumption are now predicates on the UPDATE, where the database adjudicates them: a conditional increment returns zero rows once the cap is reached, and consumption is a `whereNull('consumed_at')` claim exactly one racer wins. The second half was found while fixing the first — the same read-then-write let two requests carrying the SAME correct code both pass `Hash::check` and both stamp `consumed_at`, minting two sessions from one code. Tests pin the SQL predicate rather than the behaviour, because `usable()` hides both bugs from any sequential test — a behavioural test goes green on the old code. Mutation-checked both ways — 2026-08-25. ORIGINAL: Read, then increment, then compare: N parallel guesses all pass `usable()`. The real cap is the route throttle (~200 over the code's life), not 5.
- [x] **Frontend — DataImport's wizard can never be completed.** Guard now uses `steps.length`. Driving the flow ALSO turned up two backend bugs: `store()` returning false was unchecked (a failed disk write produced an import row pointing at a file named "0", with empty headers and every later step failing for an unrelated-looking reason), and `fgetcsv`'s deprecated `$escape` default. Full chain verified: upload → dry-run → run imports 2/2 rows — 2026-08-25. ORIGINAL: `onFinish` guards `step === 3` but the Finish button only emits at `step === 4`. "Start import" does nothing: no request, no error.
- [x] **Frontend — RolesPermissions loses permissions across groups.** Replaced the per-group chip-groups with plain chips and an explicit toggle. My first fix had its own browser-only bug: `:model-value` on a STANDALONE v-chip is existence, not selection, so every unselected chip vanished. Verified: ticking across two groups keeps both, and the saved role shows "2 granted" — 2026-08-25. ORIGINAL: One `v-chip-group` per group, all bound to the same array; Vuetify's group transform drops values it does not own, so ticking a permission in a second group clears the first.
- [x] **Frontend — forgot-password feedback is invisible.** `:messages` + `active`, not `v-model`. Verified in the browser: the hint now renders where an empty div used to be — 2026-08-25. ORIGINAL: `<v-messages v-model=...>` — VMessages has no `modelValue`; it takes `:messages` + `active`. Every error on that path is silently dropped.
- [x] **Frontend — four empty states pass `text=` to `AppEmptyState`, which declares `description`.** All four swept — 2026-08-25. ORIGINAL: The explanatory line never renders (Comments, Tasks, Announcements, FormBuilder).
- [x] **Smaller, batched** — all 13 items, 2026-08-25. Two were already fixed as side effects of earlier leaves (Announcements re-dismiss; Booking/Support reference entropy) and were verified rather than re-fixed. The rest: **SetPasswordPage's dead `validate()` guard** (VForm.validate() resolves to an object, so `!await …` was always false — swept every call site across the kernel and all 22 modules, this was the last one). **Comments edit/delete never re-checked the parent's ability** — ownership does not expire but access does, and editing re-syncs mentions, so a removed user kept notifying people inside a record they could no longer open; the parent check runs BEFORE ownership so the ordering is not itself an oracle. **403-vs-404 record oracles** in Comments/Tags/Favorites and **401-vs-404** in FormBuilder — sequential ids plus a status-code difference enumerates the table; FormBuilder's refusal carries the sign-in hint so a real visitor is not stuck. **The global tag pool had no gate at all** — now a `TagPoolScope` seam (third use of this pattern), permissive by default, documented. **Presigned uploads let the caller pick the S3 key prefix** — now an allow-list. **Password reset returned the raw User model** (same bug as AuthUserResource, different endpoint) **and undid sendResetLink's deliberate refusal to confirm an address**. **Raw driver text reached the user** from Exports/DataImport — deliberate AppExceptions still pass through, everything else becomes a logged reference, mutation-checked in BOTH directions so over-redacting fails too. **SettingsManager was a singleton**, so a queue worker served the settings it booted with — `scoped()`, plus a bounded TTL so one dropped cache delete is not permanent. **A failed invitation send destroyed the invitation it replaced** — both store() and resend() are transactional now. **Files was a hardcoded-colour island** — its `--v3-dropzone--*` variables held literal RGB triplets, which reads as theming while a brand change did nothing; swept every module (Files was the only one) and `bin/lint` now greps for it, mutation-checked by reintroducing a literal.

## Modules — decisions the research forced


- [ ] **Tenancy sequencing** — SavedViews shipped with a `SavedViewScope` seam (bind once, whole module covered) — that is the pattern the remaining per-user modules should copy, and it makes this decision cheap to defer rather than free to ignore. Comments and Tasks are queued and both store per-user rows; AuditLog already shipped. If the firm wants tenant scoping, deciding late means retrofitting each. Not a blocker today (AuditLog reads through a project-defined gate, so a tenant-aware project scopes there), but it is Devin's call before the next per-user module.
- [x] **Otp depends on an SMS channel** — RESOLVED by the seam pattern three modules have now used (AudienceResolver, SavedViewScope, CommentableRegistry): Otp declares an `OtpChannel` contract and ships email; a project binds Twilio or anything else. No SmsChannel module has to land first, and nothing about Twilio is baked in — 2026-08-24
- [x] **SsoAuth is an Auth option** — shipped as a SEPARATE `sso` axis rather than a third choice on `auth`, deviating from the research note: the two point opposite ways (`auth=sanctum+oauth` makes the app an OAuth SERVER for MCP clients, `sso=oidc` an OAuth CLIENT of Google/Microsoft), so one setting would force a false choice. Allow-listed providers, module-issued single-use state (stateless() drops Socialite's CSRF and API routes have no session), verified-email-only account linking, registration off by default, deactivated users refused. 19 SSO tests, 33 across the module — 2026-08-24
- [x] **SAML as a third `sso` choice** — shipped 2026-08-24 as `sso = none | oidc | saml | oidc+saml`. Both protocols end at the same `SsoIdentityResolver` and the same single-use handoff code, so the account rules are written once; the resolver now takes a protocol-neutral `SsoIdentity` rather than a Socialite user. 26 tests minting REAL signed assertions against a throwaway self-signed IdP — every negative case is a genuinely hostile document (unsigned, wrong key, tampered after signing, replayed, expired, wrong audience, wrong issuer, answering a different request). All four variants green: 14 / 44 / 40 / 70. Not built, and said so: Single Logout, encrypted assertions. ORIGINAL NOTE: — deliberately not folded into the OIDC option: it means `onelogin/php-saml` or `simplesamlphp` and a different protocol surface (ACS URL semantics, SP- vs IdP-initiated, `RequestedAuthnContext` defaults). Evidence is the mibev Marriott PingFederate integration — a 189-iteration session that ended marked failed, where the root cause was an enterprise device-cert requirement but most of the time went to exactly those semantics. A module plus a troubleshooting doc is what retains that.

## Project context docs


`.claude/context/` scaffolded 2026-08-24 during the module run. Only the phases the run needed were completed.

- [x] Phase 0 scaffold + `## Project Context` block injected into CLAUDE.md — 2026-08-24
- [x] Phase 9 roadmap seeded as the run's work registry — 2026-08-24
- [ ] Phases 2-8, 10 — product, glossary, personas, features, workflows, infrastructure/SSH access, integrations, runbooks, ADRs. Several need Devin present (SSH keys, account owners, business model).

## Template bootstrap — RolesPermissions is bundled but never wired


- [!] **The template bundles RolesPermissions and does not put `HasAccessControl` on its User model, so out of the
  box no user can hold a role.** Consequence, measured by driving it: `/roles` manages roles nobody can be given,
  and every gated module page answers 403 — announcements, settings, audit log, invitations, forms admin, users.
  A fresh bootstrap therefore looks broken until someone reads step 3 of the module README. The module is
  correctly defensive about it (every caller guards with `method_exists`, and `GrantAdminCommand` prints the
  README path), so this is not a bug in the module.
  blocked-on: **Devin's call.** Adding the trait to the kernel User couples it to a removable module — deleting
  RolesPermissions would then fatal on a missing trait, which is precisely why it is a seam. Leaving it means the
  template ships a decorative permissions UI. A third option is a `HasAccessControl` shim in the kernel that the
  module overrides, which keeps User compiling either way but adds a concept.
  Evidence: with the trait added temporarily, all six of those pages render and work — announcements created and
  published end to end, and unpublish→republish correctly did not re-mail (deliveries stayed at 2).

## Dependencies — routine bumps


- [ ] **15 open Dependabot PRs on the template, none security.** `npm audit` and `composer audit` both report
  zero advisories, so these are version currency rather than risk. All are within-major (Vue 3.5.x tooling,
  guzzle 7.10→7.15, sass, vitest, dayjs, flysystem, sentry-laravel); four are grouped PRs. Deliberately NOT
  force-updated during the overnight run: several change package.json CONSTRAINTS rather than just the lock, and
  rewriting the locks here would conflict with all 15 PRs and send Dependabot into a rebase loop. They merge
  cleanly through CI, which is the cheaper path.

## Recently Done (last 30 days)


- **Realtime — module 28** — all leaves shipped 2026-08-25. Reverb/Echo websocket wiring — broadcast auth behind Sanctum (not the `web` guard), client config served rather than baked in for Capacitor, channel guards, connection banner; the drop-list guard came out of it.
- **Checklists — module 27** — all leaves shipped 2026-08-25. Reusable checklist templates + instances against any subject, with optional file evidence; the `evidence=none` variant exposed the static-import hazard that became `bin/check-drop-imports.py`.
- **Onboarding — module 25** — all leaves shipped 2026-08-25. Registry-driven setup steps with a `onboarded` middleware gate and a dismissible banner; two mutations survived the first test pass and both got real tests.
- **GlobalSearch — module 24** — all leaves shipped 2026-08-25. Cross-entity search palette (Cmd/Ctrl+K) over a source registry, optional per-user history and Scout backing.
- **Render-correctness guards — the failures no linter can see** — all leaves shipped 2026-08-25. Seven greps in `bin/lint-kernel`, mirrored into the modules repo — mdi names, v3 typography, hardcoded colours, unlabelled icon buttons, Vuetify 2 class names, unknown utility classes, unimported components. Each one born from an accident that had already happened.
- **Modules — generic verticals (built fresh, washwerk as design reference only)** — all leaves shipped 2026-08-24. Twelve verticals built generic from scratch with washwerk read only as a design reference.
- **Icons — the whole surface was using the wrong set** — all leaves shipped 2026-08-24. Material Symbols ligature names throughout; `mdi-*` names render as literal text under this icon set and nothing warns.
- **Module frontends were never type-checked** — all leaves shipped 2026-08-24. vue-tsc extended over `modules/`, and the errors that surfaced fixed.
- **A module could not replace a kernel route** — all leaves shipped 2026-08-24. `moduleProvides()` guard so a module route wins over the kernel placeholder instead of being shadowed by it.
- **Modules — evidence-ranked candidates (research 2026-08-24)** — all leaves shipped 2026-08-24. Candidate modules ranked by evidence across the org's real repos, then built or explicitly declined.
- **Modules — candidate discovery** — all leaves shipped 2026-08-24. Swept the organisation's repo inventory for recurring verticals worth extracting.
- **Template health** — all leaves shipped 2026-08-24. Baseline template cleanup — the P0 items from the first audit pass.
- **Vuetify v4 typography was dead across the whole app** — shipped 2026-08-25. `text-h4`, `text-body-2`,
  `text-caption` and the rest do not EXIST in v4 (renamed to the MD3 scale), so 81 usages across the kernel and
  22 modules rendered at the inherited size and every heading fell back to a browser default. Nothing errored.
  The v4 upgrade had already removed the `#applied { h1 { @extend .text-h1 } }` mapping, and the follow-up that
  added `class="text-h3"` to the login title was dead too. Remapped by measured size/weight, guarded in bin/lint.
- **Accessibility pass over the public and auth surface** — shipped 2026-08-25. 19 icon-only buttons announced
  nothing (Vuetify renders `icon="delete"` as a ligature, so the only text content is the glyph); 7 pages had no
  h1 or started at h3, and every one was public or auth; the three password-reveal toggles had `tabindex="-1"`,
  so the only way to check a typed password was unreachable by keyboard; /login had no autocomplete or type on
  its inputs. Also: board cards advertised a click that did nothing, and the file dropzone's preview was
  mouse-only.

- **Modules — browser verification sweep** — all leaves shipped 2026-08-25 (every module page loaded and looked at,
  not just 200-checked, at desktop and 390px). What it found, none of which any suite saw: the Booking confirmation
  named the BOOKER as the location (`$this->resource` in a JsonResource is the wrapped model, and Booking has a
  relation called `resource`); the kanban board had no link in or out; every module page's browser tab showed its
  route identifier (`booking.show` on a public page); `/notifications` scrolled sideways at 390px, and ten more
  header rows were one long label away from the same. Two things I first read as bugs were NOT: four "blank" pages
  were a 2s settle being too short, and two 404s are correct because `import.meta.glob` already guards route
  registration. Public booking and public form flows both driven end to end, logged out.

- **Option drop-list symmetry** — all leaves shipped 2026-08-25 (three variants dropped a TEST while keeping the
  code it covered, and it was the DEFAULT choice carrying the drop in each — Comments `threading=flat` 28→33,
  Booking `approval=instant` 36→42, Billing `admin=none` 60→64. `bin/check-drop-symmetry` gates the mechanical
  half from the CI discover job; Auth `sso=saml` declared as the one exception with its evidence).
- **Template bundle variant drift** — all leaves shipped 2026-08-25 (Billing was bundled at `admin=switcher`, the
  QA entitlement-switcher variant, left over from a verification run and inert only because the route self-gates.
  `module:check --defaults` now compares installed_options against each manifest's stated default; template-only
  flag so client projects, which choose variants on purpose, are unaffected).

- Auth SAML deferrals — all leaves shipped 2026-08-24 (both re-questioned and both real: the metadata had been ADVERTISING an SLS endpoint that served nothing, and encryption was wired but never exercised. Building SLO turned up an unauthenticated log-anybody-out — php-saml only requires a LogoutRequest signature when `wantMessagesSigned` is set, which is correctly optional for sign-in but fatal for a message that carries no assertion. Auth's "Not included" list is now empty for SAML.).
- SSO security review — all leaves shipped 2026-08-24 (six findings from an adversarial review of the OIDC option, closing with the issuer pin: `allowed_domains` cannot catch nOAuth, because on a multi-tenant endpoint the attacker asserts YOUR domain from THEIR tenant and the domain check passes by design. `required_claims` pins the tenant claim, fails closed on an absent claim and on an env var that did not resolve, and applies to already-linked identities so adding a pin actually revokes. SAML needed nothing — php-saml pins `<Issuer>` to `idp.entityId` before reading the assertion.).
- Auth module type-error bugs — all leaves shipped 2026-08-24 (decorative client-side validation on login and forgot-password, an error string bound to a boolean visibility flag, and the structural gap behind both: module frontends were only type-checked once copied into a template checkout, so nothing ever did it. `vue-tsc --noEmit` now runs on every leg of the module CI matrix.).
- Modules — Favorites — all leaves shipped 2026-08-24 (an org-wide sweep of 445 repos re-grounded the local rankings: no contradiction, one new candidate, and Support demoted; Favorites then built as module #22).
- SSO shipped 2026-08-24 as an Auth option — and the browser found two bugs the tests had not: a provider listed without credentials rendered a button that threw on click (the option's own env default shipped that state), and the callback echoed any throwable's message, sending raw SQL to the client when a migration had not run.
- User payload allow-listed 2026-08-24 — AuthUserResource returned the whole model, so any module adding a `users` column shipped it to every client (the Users module already had). Five explicit fields, and the roles/permissions checks no longer lazy-load.
- Template CI is live again 2026-08-24 — nothing had gated a PR; now pint+pest, eslint+vue-tsc+vitest, and a Capacitor build smoke all run on every pull request.
- `module:make` 2026-08-24 — new modules start from the canonical shape, green on every check before a line is edited.
- Frontend type safety — 42 vue-tsc errors to 0 on 2026-08-24, and the six real bugs the broken declarations were hiding (unauthenticated autocomplete creates, an edit form that silently wiped records, a reversed `$confirm` signature).
- Modules — extraction from the template — all leaves shipped 2026-08-24 (Files and Users lifted out of the kernel; the kernel keeps only a read-only `users` typeahead).
- Kernel slot-forwarding bug fixed 2026-08-24 — six components blanked the ENTIRE page when the wrapped Vuetify component invoked a zero-argument slot (`no-data`, `loading`, `top`, `bottom`). Any list screen using `#no-data` was dead. Pinned by a vitest spec that reproduces the throw.
- Modules CI harness — went from every-run-red to fully green 2026-08-24 (4 real bugs: public-repo token, phantom COMPOSER_AUTH, unbuilt frontend, Example collision).
